import 'package:flutter/material.dart';

class Module1A extends StatelessWidget {
  const Module1A({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      title: const Text('Module 1A'),
      centerTitle: true,
    ),
  );
}