import 'package:flutter/material.dart';

class Module2B extends StatelessWidget {
  const Module2B({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      title: const Text('Module 2B'),
      centerTitle: true,
    ),
  );
}