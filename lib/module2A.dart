import 'package:flutter/material.dart';

class Module2A extends StatelessWidget {
  const Module2A({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      title: const Text('Module 2A'),
      centerTitle: true,
    ),
  );
}