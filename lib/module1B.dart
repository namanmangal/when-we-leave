import 'package:flutter/material.dart';

class Module1B extends StatelessWidget {
  const Module1B({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      title: const Text('Module 1B'),
      centerTitle: true,
    ),
  );
}